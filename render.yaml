# =============================================================
# PLATFORM — Render Blueprint (Free Tier Optimized)
# 1 Backend unificado + N Static Sites por cliente
# =============================================================

services:

  # ─────────────────────────────────────────────
  # BACKEND CENTRALIZADO — Todos los clientes
  # ─────────────────────────────────────────────
  - type: web
    name: platform-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npx tsc --outDir dist
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: REQUIRE_AUTH
        value: "true"

  # ─────────────────────────────────────────────
  # CLIENTE: ZENKO — Arreglos de Ropa (Ana & Ariel)
  # ─────────────────────────────────────────────
  - type: web
    name: zenko-app
    runtime: static
    rootDir: clients/zenko
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ─────────────────────────────────────────────
  # CLIENTE: DEMO TEST (v2 Platform Web)
  # ─────────────────────────────────────────────
  - type: web
    name: platform-v2-demo-test
    runtime: static
    rootDir: platform-v2/apps/web
    buildCommand: npm install --legacy-peer-deps && VITE_TENANT=demo_test npx vite build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
      - key: VITE_TENANT
        value: demo_test
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ─────────────────────────────────────────────
  # BACKEND: DEMO TEST (v2 Platform Backend)
  # ─────────────────────────────────────────────
  - type: web
    name: platform-v2-backend
    runtime: node
    plan: free
    rootDir: platform-v2/apps/backend
    buildCommand: npm install --legacy-peer-deps && npx tsc
    startCommand: node dist/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false

  # ─────────────────────────────────────────────
  # CLIENTE: MG MASAJES — Masajes y Bienestar
  # ─────────────────────────────────────────────
  - type: web
    name: mg-masajes-app
    runtime: static
    rootDir: clients/mg_masajes
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
